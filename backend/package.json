{
  "name": "golden-table-backend",
  "version": "1.0.0",
  "description": "Backend API for The Golden Table voice assistant",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch server.js",
    "build": "node server.js",
    "vercel-build": "echo hello"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  }
}